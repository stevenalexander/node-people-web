extends layout

block content
  h1= title
  div
    ul
    each person in people
      li
        span= person.name
        | &nbsp;
        a(href='/delete/' + person.id) x

  div
    form(action='/add', method='post')
      input(name='name')
      input(type='submit', value='add')
